# manage.bhzn.top —— 项目管理与 AI 赋能独立站
# 80 段留给 certbot 做 HTTP-01 校验；证书签发后再启用 443 段。
server {
    listen 80;
    server_name manage.bhzn.top;
    location /.well-known/acme-challenge/ { root /var/www/html; }
    location / { return 301 https://$host$request_uri; }
}
